슬라이드대로 따라간 후, //~13p
cal.l, cal.y를 c로 재생성 및 cal_tab.h를 재생성한다.

.c파일은 소스파일에, .h는 헤더파일에 추가해준다.

헤더파일로 
<오류목록>
'NAME': 선언되지 않은 식별자입니다.
'yylval': 선언되지 않은 식별자입니다.
'yyerror': 재정의. 기본 형식이 다릅니다.
'NUMBER': 선언되지 않은 식별자입니다.
를 모두 해결 가능하다.  

<참고>
-yylval: yacc에서 사용하는 변수 
-#include <>는 표준라이브러리에 있는거, ""는 사용자정의 헤더파일.
-""는 소스파일과 같은경로에 있는지 먼저 찾아봄. <>는 그 위치까지 찾아가서 추가함.
따라서 사용자정의헤더는 파일위치 꼭 확인하기.

<실행결과>
4+6하고 엔터하면 아무것도 안 뜨다가, s치고 보면 

=10
parse error
이렇게 결과가 나온다. 

why?

왜 c는 항상 끝에 세미콜론을 붙이는가를 고민해보자,
-> 끝에 항상 ;를 붙이도록 구문을 고쳐보자

=> cal.y contains 20 shift/reduce conflicts 가 사라진 모습을 볼 수 있다.

=> 상황해석 : cal.y가 shift reduce conflicts를 갖고있다는 의미 = 모호한 문법이라는 의미

-가 들어왔을때 이항연산자인지 단항연산자인지 모호.

<추가 메모>
yylval에 대해 설명
: 비손에서 정의해서 쓰는 변수. 따라서 cal_tab.h에서 extern선언 해 준 것을 볼 수 있었음

cal_tab.h를 include하는 파일들은 yylval을 쓸 수 있게됨.

yylval은, 토큰을 리턴하는 것 뿐 아니라 그 외 실제 토큰의 값을 같이 넘겨줘야 하므로 (의미 분석 단계에서 써야 해서). 그 값을 임시 저장하기 위해 yylval을 쓴다.

yylval=atoi(yytext): yylval 에 토큰값 저장하는데, yytext는 string이니까 int로 형변환한다는 뜻.

<warning도 해결해보자>
cal.y에다가 함수원형 써주기
: 

int yyerror(const char* s);
extern int yyparse();

=> c에서 함수는 사용하기 전에 상단에서 선언해주는 것이 좋음

<추가 팁>
lex, yacc를 빌드해서 생성된 c를 수정하려하지말고, .l 및 .y파일을 수정해라

<우선순위설정: conflict 해결>
%left '-' '+'
%left '*' '/'
%nonassoc UMINUS

아래쪽으로갈수록 우선순위 높음.

| '-' expr ';'	 %prec UMINUS 이렇게 수정해준 상태//cal,y
: 우선순위를 높여준것임(모호한 -구문들에대해)

최종적으로, ';'를 다 붙여주는 게 아니라, 연산자의 우선순위만 고쳐줘도 shift/refuce conflicts에러가 사라진 것을 알 수 있다.

<오류메세지정의>
| expr '/' expr 		{if ($3 ==0) yyerror("divide by zero");
						else $$ = $1 / $3;}

<4+6은 결과나오는데 왜 4*3은 parse error가 뜰까>
=> 
입력이 4+6 \n 4*3 \n 이렇게 들어온것임.
근데, 첫번째꺼까지는 expr '\n'적용되는데, 뒤에부터는 적용될 게 없어서 파스에러
<
result = 4 
+5
하면 왜 parse error가 뜰까>

<다음시간에 에러 해석 해 주신다 함..>
